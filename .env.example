# Copy this file to `.env` in the project root and set your key.
# The Study Agent server reads `.env` and injects the key when you open
# http://localhost — you will not need to paste it in the browser each time.
#
# Never commit `.env` (it is gitignored).

ANTHROPIC_API_KEY=
